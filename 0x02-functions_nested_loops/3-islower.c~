#include <stdio.h>
#include "main.h"
/**
 * print_alphabet_x10(:)? (- print all alphabets in lowercase 10 times)?
 */
int _islower(int c)
{
  if (c >= 97 && c <= 122)
    return (1);
  else
    return (0);
}

