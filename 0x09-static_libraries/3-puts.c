#include <stdio.h>
#include "main.h"
/**
 * _puts - print a string with neew line
 * @str: is the string
 * Return: the string with new line
 */
void _puts(char *str)
{
while (*str != '\0')
{
str++;
}
}
